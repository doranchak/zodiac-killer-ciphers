package com.zodiackillerciphers.ciphers.generator;

/** compute all the metrics for the given candidate key. */
public class Evaluator {
	public CandidateKey key;
	
	/** return array of scores.  if any constraint is violated, severely reduce the score. */
	public float[] evaluate() {
		return null;
	}
	
}
